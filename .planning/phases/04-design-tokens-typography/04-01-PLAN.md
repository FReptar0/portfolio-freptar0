---
phase: 04-design-tokens-typography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/globals.css
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Every page renders headings in Space Grotesk, body in DM Sans, code/labels in JetBrains Mono"
    - "No Geist font loads or renders anywhere on the site"
    - "No blue or cyan color appears in the CSS custom properties"
    - "The only accent color defined is electric green (#22C55E) and its tonal variants"
    - "Dark mode uses near-black backgrounds (#0A0A0A/#111111), not navy (#0F172A)"
    - "Glass class names still exist in CSS so components do not break, but they render as clean surfaces"
    - "All existing pages load without hydration errors or missing styles"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Space Grotesk, DM Sans, JetBrains Mono font loading via next/font/google"
      contains: "Space_Grotesk"
    - path: "app/globals.css"
      provides: "Complete Swiss design token system with light/dark modes"
      contains: "--color-accent"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS variable names for fonts (--font-heading, --font-body, --font-mono)"
      pattern: "--font-heading|--font-body|--font-mono"
    - from: "app/globals.css :root"
      to: "app/globals.css .dark"
      via: "Same variable names with different values"
      pattern: "--color-bg|--color-fg|--color-accent"
    - from: "app/globals.css"
      to: "components/**/*.tsx"
      via: "var() references in inline styles inherit new values"
      pattern: "var\\(--"
---

<objective>
Replace the entire visual DNA of the portfolio: swap Geist fonts for the Swiss typography stack (Space Grotesk / DM Sans / JetBrains Mono) and rebuild all CSS custom properties from blue-cyan-glass to black/white/green-clean.

Purpose: This is the foundation that every component inherits from. Changing these tokens changes the entire site's visual identity in one pass, because all components reference CSS custom properties.

Output: Updated app/layout.tsx with new fonts, completely rewritten app/globals.css with Swiss design tokens, clean dark mode, and backward-compatible glass class stubs.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace font loading in layout.tsx</name>
  <files>app/layout.tsx</files>
  <action>
Replace the Geist font imports and configuration with three Google Fonts via next/font/google:

1. REMOVE these imports and declarations:
   - `import { Geist, Geist_Mono } from "next/font/google";`
   - `const geistSans = Geist({ ... })`
   - `const geistMono = Geist_Mono({ ... })`

2. ADD these imports and declarations:
   ```
   import { Space_Grotesk, DM_Sans, JetBrains_Mono } from "next/font/google";

   const spaceGrotesk = Space_Grotesk({
     variable: "--font-heading",
     subsets: ["latin"],
     display: "swap",
     weight: ["400", "500", "600", "700"],
   });

   const dmSans = DM_Sans({
     variable: "--font-body",
     subsets: ["latin"],
     display: "swap",
     weight: ["400", "500", "700"],
   });

   const jetbrainsMono = JetBrains_Mono({
     variable: "--font-mono",
     subsets: ["latin"],
     display: "swap",
     weight: ["400", "500", "700"],
   });
   ```

3. UPDATE the body className:
   FROM: `${geistSans.variable} ${geistMono.variable} antialiased`
   TO: `${spaceGrotesk.variable} ${dmSans.variable} ${jetbrainsMono.variable} antialiased`

Do NOT change anything else in layout.tsx (ThemeProvider, SpeedInsights, Analytics, locale handling, inline theme script — all stay exactly as they are).
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && grep -c "Geist" app/layout.tsx | grep -q "^0$" && grep -q "Space_Grotesk" app/layout.tsx && grep -q "DM_Sans" app/layout.tsx && grep -q "JetBrains_Mono" app/layout.tsx && grep -q "font-heading" app/layout.tsx && grep -q "font-body" app/layout.tsx && grep -q "font-mono" app/layout.tsx && echo "PASS: Fonts replaced correctly"</automated>
  </verify>
  <done>layout.tsx loads Space Grotesk, DM Sans, and JetBrains Mono via next/font/google with CSS variable names --font-heading, --font-body, --font-mono. Zero Geist references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild CSS custom properties, dark mode, and glass fallbacks</name>
  <files>app/globals.css</files>
  <action>
Completely rewrite the CSS custom properties in globals.css. Keep the file structure (imports at top, then :root, @theme inline placeholder comment, .dark, body, utility classes, logoloop, base layer) but replace all the color/glass/gradient content.

**IMPORTANT CONTEXT:** There are 269 occurrences of `var(--primary-600)`, `var(--accent-500)`, `var(--glass-bg)`, etc. across 15 component files. These components will NOT be updated in this phase. To prevent breakage, we MUST keep backward-compatible aliases for the old variable names, pointing them at the new values. This is critical.

**Section 1: :root block (replace lines 5-147)**

New `:root` should contain:

```css
:root {
  /* === Swiss Design Tokens === */

  /* Base Colors */
  --color-bg: #FFFFFF;
  --color-fg: #000000;
  --color-accent: #22C55E;
  --color-accent-hover: #16A34A;
  --color-accent-muted: rgba(34, 197, 94, 0.1);

  /* Surfaces */
  --color-surface: #FFFFFF;
  --color-surface-elevated: #FAFAFA;
  --color-surface-sunken: #F5F5F5;

  /* Text */
  --text-primary: #000000;
  --text-secondary: #525252;
  --text-tertiary: #A3A3A3;
  --text-inverse: #FFFFFF;

  /* Borders */
  --color-border: rgba(0, 0, 0, 0.08);
  --color-border-strong: rgba(0, 0, 0, 0.16);

  /* Backgrounds (legacy names mapped to new values) */
  --bg-primary: #FFFFFF;
  --bg-secondary: #FAFAFA;
  --bg-tertiary: #F5F5F5;

  /* Shadows — clean, no glass purple tint */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.04);
  --shadow-md: 0 2px 8px -2px rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 8px 24px -4px rgba(0, 0, 0, 0.12);
  --shadow-xl: 0 16px 40px -8px rgba(0, 0, 0, 0.16);

  /* State Colors */
  --color-success: #22C55E;
  --color-warning: #EAB308;
  --color-error: #EF4444;
  --color-info: #3B82F6;

  /* Spacing tokens (8px grid) */
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;
  --space-16: 4rem;
  --space-24: 6rem;
  --space-32: 8rem;

  /* Border radius */
  --radius: 0.5rem;
  --radius-sm: 0.25rem;
  --radius-md: 0.375rem;
  --radius-lg: 0.5rem;
  --radius-xl: 0.75rem;
  --radius-2xl: 1rem;
  --radius-full: 9999px;

  /* === Backward Compatibility Aliases === */
  /* These map old variable names to new values so existing components don't break.
     Phase 5 will remove these when components are rewritten. */

  /* Old primary blue scale -> now accent green for prominent uses, neutral for subtle */
  --primary-50: #F0FDF4;
  --primary-100: #DCFCE7;
  --primary-200: #BBF7D0;
  --primary-300: #86EFAC;
  --primary-400: #4ADE80;
  --primary-500: #22C55E;
  --primary-600: #22C55E;
  --primary-700: #16A34A;
  --primary-800: #166534;
  --primary-900: #14532D;

  /* Old accent cyan scale -> mapped to accent green */
  --accent-50: #F0FDF4;
  --accent-100: #DCFCE7;
  --accent-200: #BBF7D0;
  --accent-300: #86EFAC;
  --accent-400: #4ADE80;
  --accent-500: #22C55E;
  --accent-600: #16A34A;
  --accent-700: #15803D;
  --accent-800: #166534;
  --accent-900: #14532D;

  /* Old secondary orange -> mapped to neutral warm gray (de-emphasized) */
  --secondary-50: #FAFAFA;
  --secondary-100: #F5F5F5;
  --secondary-200: #E5E5E5;
  --secondary-300: #D4D4D4;
  --secondary-400: #A3A3A3;
  --secondary-500: #737373;
  --secondary-600: #525252;
  --secondary-700: #404040;
  --secondary-800: #262626;
  --secondary-900: #171717;

  /* Old state colors aliased */
  --success-50: #F0FDF4;
  --success-500: #22C55E;
  --success-600: #16A34A;
  --success-700: #15803D;

  --warning-50: #FEFCE8;
  --warning-500: #EAB308;
  --warning-600: #CA8A04;
  --warning-700: #A16207;

  --error-50: #FEF2F2;
  --error-500: #EF4444;
  --error-600: #DC2626;
  --error-700: #B91C1C;

  --info-50: #F0FDF4;
  --info-500: #22C55E;
  --info-600: #16A34A;
  --info-700: #15803D;

  /* Old glass vars -> clean surface equivalents (no blur, no transparency) */
  --glass-bg: rgba(255, 255, 255, 0.95);
  --glass-border: var(--color-border);
  --glass-shadow: rgba(0, 0, 0, 0.04);

  /* Old blur values -> kept but will be unused as glass classes are simplified */
  --blur-sm: 0px;
  --blur-md: 0px;
  --blur-lg: 0px;
  --blur-xl: 0px;

  /* Old border aliases */
  --border-subtle: var(--color-border);
  --border-medium: var(--color-border-strong);
  --border-strong: rgba(0, 0, 0, 0.24);

  /* Old gradient vars -> solid colors (kill the gradients) */
  --gradient-mesh: none;
  --gradient-hero: var(--color-accent);
  --gradient-accent: var(--color-accent);
  --gradient-glass-light: none;

  /* Old legacy aliases */
  --primary-blue: var(--color-accent);
  --accent-purple: var(--color-accent);
  --success-green: var(--color-success);
  --growth-orange: var(--text-secondary);

  /* Shadow alias */
  --shadow-2xl: 0 24px 48px -12px rgba(0, 0, 0, 0.2);
  --shadow-glass: var(--shadow-md);

  /* === shadcn/ui Compatibility Tokens (oklch) === */
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  --primary: oklch(0.145 0 0);
  --primary-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.965 0 0);
  --secondary-foreground: oklch(0.145 0 0);
  --muted: oklch(0.965 0 0);
  --muted-foreground: oklch(0.45 0 0);
  --accent: oklch(0.965 0 0);
  --accent-foreground: oklch(0.145 0 0);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);
  --ring: oklch(0.55 0.17 142);
  --chart-1: oklch(0.646 0.222 41.116);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  --sidebar: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.145 0 0);
  --sidebar-primary: oklch(0.145 0 0);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.965 0 0);
  --sidebar-accent-foreground: oklch(0.145 0 0);
  --sidebar-border: oklch(0.922 0 0);
  --sidebar-ring: oklch(0.55 0.17 142);
}
```

Key decisions in the above:
- `--ring` uses oklch green-ish hue (142 = green) so focus rings use accent color
- shadcn tokens use achromatic oklch (no hue) for true black/white neutrals
- All old blue/cyan variables now resolve to green equivalents
- Glass vars resolve to near-opaque white (clean surface, not transparent blur)
- Gradient vars resolve to solid accent color or `none`
- Info color mapped to green (matches accent) instead of old cyan

**Section 2: .dark block (replace lines 194-247)**

```css
.dark {
  /* Swiss Dark Mode — deliberately designed, not inverted */
  --color-bg: #0A0A0A;
  --color-fg: #FAFAFA;
  --color-accent: #22C55E;
  --color-accent-hover: #4ADE80;
  --color-accent-muted: rgba(34, 197, 94, 0.15);

  --color-surface: #111111;
  --color-surface-elevated: #1A1A1A;
  --color-surface-sunken: #0A0A0A;

  --text-primary: #FAFAFA;
  --text-secondary: #A3A3A3;
  --text-tertiary: #525252;
  --text-inverse: #000000;

  --color-border: rgba(255, 255, 255, 0.08);
  --color-border-strong: rgba(255, 255, 255, 0.16);

  --bg-primary: #0A0A0A;
  --bg-secondary: #111111;
  --bg-tertiary: #1A1A1A;

  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.2);
  --shadow-md: 0 2px 8px -2px rgba(0, 0, 0, 0.3);
  --shadow-lg: 0 8px 24px -4px rgba(0, 0, 0, 0.4);
  --shadow-xl: 0 16px 40px -8px rgba(0, 0, 0, 0.5);
  --shadow-2xl: 0 24px 48px -12px rgba(0, 0, 0, 0.6);
  --shadow-glass: var(--shadow-md);

  --border-subtle: var(--color-border);
  --border-medium: var(--color-border-strong);
  --border-strong: rgba(255, 255, 255, 0.24);

  --glass-bg: rgba(17, 17, 17, 0.95);
  --glass-border: var(--color-border);
  --glass-shadow: rgba(0, 0, 0, 0.3);

  --gradient-glass-light: none;

  /* shadcn/ui dark tokens */
  --background: oklch(0.08 0 0);
  --foreground: oklch(0.985 0 0);
  --card: oklch(0.12 0 0);
  --card-foreground: oklch(0.985 0 0);
  --popover: oklch(0.12 0 0);
  --popover-foreground: oklch(0.985 0 0);
  --primary: oklch(0.922 0 0);
  --primary-foreground: oklch(0.08 0 0);
  --secondary: oklch(0.17 0 0);
  --secondary-foreground: oklch(0.985 0 0);
  --muted: oklch(0.17 0 0);
  --muted-foreground: oklch(0.55 0 0);
  --accent: oklch(0.17 0 0);
  --accent-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.65 0.2 142);
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
  --sidebar: oklch(0.12 0 0);
  --sidebar-foreground: oklch(0.985 0 0);
  --sidebar-primary: oklch(0.488 0.243 264.376);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.17 0 0);
  --sidebar-accent-foreground: oklch(0.985 0 0);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.65 0.2 142);
}
```

Key dark mode decisions:
- Background is #0A0A0A (near-black, NOT navy #0F172A)
- Surfaces are true gray (#111111, #1A1A1A), not blue-tinted
- Green accent stays vibrant — same #22C55E
- Muted text is #A3A3A3 (neutral gray)
- All oklch tokens are achromatic (no hue) for pure neutrals

**Section 3: body styles (replace lines 249-256)**

```css
body {
  background: var(--color-bg);
  color: var(--text-primary);
  font-family: var(--font-body), 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
  font-size: 16px;
  line-height: 1.6;
  overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```

Changes: Use --font-body (DM Sans) as primary font. Font size 16px (down from 18px for cleaner Swiss feel). Add font smoothing. Remove old --font-geist-sans reference.

**Section 4: Glass/gradient utility classes (replace lines 258-332)**

Keep ALL class names (.glass, .glass-card, .apple-glass, .apple-glass-dark, .bg-gradient-hero, .bg-gradient-accent, .bg-gradient-mesh, .apple-gradient-mesh) but simplify them to clean surfaces:

```css
/* Clean surface utilities (formerly glass morphism) */
/* Class names preserved for backward compatibility — Phase 5 removes usage from components */
.glass {
  background: var(--glass-bg);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-sm);
}

.glass-card {
  background: var(--glass-bg);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-sm);
  position: relative;
  transition: all 0.2s ease;
}

.glass-card::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  pointer-events: none;
  opacity: 0;
}

.glass-card:hover {
  border-color: var(--color-accent);
  box-shadow: var(--shadow-md);
}

.dark .glass-card:hover {
  border-color: var(--color-accent);
}

.apple-glass {
  background: var(--glass-bg);
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-sm);
}

.apple-glass-dark {
  background: var(--glass-bg);
  border: 1px solid var(--color-border);
}

.apple-gradient-mesh {
  background: none;
}

/* Gradient backgrounds — now solid or transparent */
.bg-gradient-hero {
  background: var(--color-accent);
}

.bg-gradient-accent {
  background: var(--color-accent);
}

.bg-gradient-mesh {
  background: none;
}
```

Key: NO backdrop-filter, NO blur, NO saturate, NO translateY(-2px) on hover (that's a Phase 5 animation concern). Glass-card hover just changes border color and shadow.

**Section 5: Focus styles (update line 359-362)**

```css
*:focus-visible {
  outline: 2px solid var(--color-accent);
  outline-offset: 2px;
}
```

Change from --primary-600 (blue) to --color-accent (green).

**Section 6: Social icon styles (update lines 364-373)**

```css
.social-icon {
  background: var(--color-surface-elevated);
  color: var(--text-primary);
}

.social-icon:hover {
  background: var(--color-accent);
  color: white;
}
```

**Section 7: LogoLoop (lines 376-572) — keep entirely unchanged.** The logoloop styles don't reference any of the old design tokens (they use their own --logoloop-* vars). Leave them as-is.

**Section 8: Base layer (lines 574-581) — keep unchanged.** This uses Tailwind's @apply with the shadcn tokens (border-border, bg-background, text-foreground) which we've already updated.

**PRESERVE:** The `@import "tailwindcss";` at line 1 and `@custom-variant dark (&:is(.dark *));` at line 3. These must remain at the very top of the file.

**PRESERVE:** Leave a comment `/* @theme inline block — see Plan 04-02 */` where the @theme inline block currently sits (lines 149-191). Do NOT modify the @theme inline block in this task — Plan 02 handles it.

ACTUALLY — on reflection, the @theme inline block MUST be present for Tailwind to work. Keep the existing @theme inline block in place for now, but add a comment `/* Updated in Plan 04-02 */` above it. The old variable references inside it (like var(--font-geist-sans)) will temporarily be broken until Plan 02 runs, but the site will still render because we set font-family directly in the body rule.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && grep -q "#22C55E" app/globals.css && grep -q "#0A0A0A" app/globals.css && grep -q "color-accent" app/globals.css && grep -c "backdrop-filter" app/globals.css | grep -q "^0$" && grep -q "font-body" app/globals.css && ! grep -q "hsla(210" app/globals.css && ! grep -q "hsla(189" app/globals.css && echo "PASS: CSS tokens rebuilt correctly"</automated>
  </verify>
  <done>globals.css contains the complete Swiss design token system: black/white base, green accent, clean dark mode with near-black backgrounds, no blue/cyan anywhere in the custom properties, glass classes simplified to clean surfaces, all old variable names aliased to new values for backward compatibility, focus ring uses green accent.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -c "Geist" app/layout.tsx` returns 0
2. `grep -c "#2563EB\|#06B6D4\|#0891B2" app/globals.css` returns 0 (no old blue/cyan hex)
3. `grep -q "#22C55E" app/globals.css` succeeds (green accent present)
4. `grep -q "#0A0A0A" app/globals.css` succeeds (dark mode near-black)
5. `grep -q "Space_Grotesk" app/layout.tsx` succeeds
6. `grep -c "backdrop-filter" app/globals.css` returns 0 (no blur effects)
7. `grep -q "font-body" app/globals.css` succeeds (body uses new font var)
</verification>

<success_criteria>
- layout.tsx loads Space Grotesk, DM Sans, JetBrains Mono (no Geist)
- globals.css :root defines Swiss tokens with green accent, no blue/cyan
- globals.css .dark uses #0A0A0A backgrounds, not navy
- Glass utility classes exist but render as clean surfaces (no blur/saturate)
- All old variable names are aliased to new values
- Focus ring uses green accent color
- The @theme inline block is still present (to be updated in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-tokens-typography/04-01-SUMMARY.md`
</output>
