---
phase: 07-homepage-trust-signals
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - locales/en/trustSignals.json
  - locales/es/trustSignals.json
  - locales/en/skills.json
  - locales/es/skills.json
  - locales/en/timeline.json
  - locales/es/timeline.json
  - components/sections/TrustSignals.tsx
autonomous: true
requirements:
  - CRED-01
  - CRED-02
  - CRED-03

must_haves:
  truths:
    - "Section previously labeled 'Certifications' is now labeled 'Technologies' in both EN and ES"
    - "No certification claims (AWS Certified, React Expert, Node.js Specialist) exist in either locale"
    - "The CertBadge component no longer uses a CheckCircle icon implying certification"
    - "Spring Boot does not appear in any SageConnect-specific context (skills project field, timeline description)"
    - "Spring Boot remains in the general Frameworks & Libraries skills list with a non-SageConnect project description"
    - "All skill proficiency levels and experience years are reasonable and internally consistent"
  artifacts:
    - path: "locales/en/trustSignals.json"
      provides: "Renamed Technologies section title, cleaned technology list"
      contains: "Technologies"
    - path: "locales/es/trustSignals.json"
      provides: "Renamed Tecnologias section title, cleaned technology list"
      contains: "Tecnolog"
    - path: "locales/en/skills.json"
      provides: "Spring Boot project field no longer references SageConnect"
    - path: "locales/es/skills.json"
      provides: "Spring Boot project field no longer references SageConnect"
    - path: "locales/en/timeline.json"
      provides: "SageConnect timeline entry uses JavaScript/Node.js, not Spring Boot"
    - path: "locales/es/timeline.json"
      provides: "SageConnect timeline entry uses JavaScript/Node.js, not Spring Boot"
    - path: "components/sections/TrustSignals.tsx"
      provides: "Technology badge without certification-implying icon"
  key_links:
    - from: "components/sections/TrustSignals.tsx"
      to: "locales/{en,es}/trustSignals.json"
      via: "useTranslations('trustSignals') for certifications array and title"
      pattern: "t\\('certificationsTitle'\\)|t\\.raw\\('certifications'\\)"
---

<objective>
Rename "Certifications & Technologies" to "Technologies", remove fake certification claims, fix Spring Boot-SageConnect false association on homepage, and audit skill accuracy.

Purpose: The homepage currently claims certifications Fernando does not hold (AWS Certified, React Expert, etc.) and falsely associates Spring Boot with SageConnect. These trust gaps damage credibility with any technical recruiter who investigates.

Output: Updated locale files with honest technology labels, corrected skills data, and fixed timeline entries in both EN and ES.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@locales/en/trustSignals.json
@locales/es/trustSignals.json
@locales/en/skills.json
@locales/es/skills.json
@locales/en/timeline.json
@locales/es/timeline.json
@components/sections/TrustSignals.tsx

<interfaces>
<!-- TrustSignals.tsx renders the certifications section (lines 158-174): -->
```tsx
<h3 ...>{t('certificationsTitle')}</h3>
<div className="flex flex-wrap justify-center gap-4">
  {t.raw('certifications').map((cert: string, index: number) => (
    <CertBadge text={cert} />
  ))}
</div>
```

<!-- CertBadge component (lines 223-229) uses CheckCircle icon: -->
```tsx
function CertBadge({ text }: { text: string }) {
  return (
    <div className="px-6 py-3 ...">
      <CheckCircle className="w-4 h-4 mr-2 inline" style={{ color: 'var(--success-500)' }} />
      {text}
    </div>
  );
}
```

<!-- EN trustSignals.json certifications array: -->
```json
"certificationsTitle": "Certifications & Technologies",
"certifications": [
  "AWS Certified", "Google Cloud", "React Expert",
  "Node.js Specialist", "TypeScript", "Docker & K8s",
  "Spring Boot", "PostgreSQL"
]
```

<!-- EN skills.json Spring Boot entry (line 65-69): -->
```json
{
  "name": "Spring Boot",
  "experience": "3 years",
  "level": 85,
  "project": "SageConnect ERP integration platform"
}
```

<!-- EN timeline.json SageConnect entry (line 21): -->
```json
"description2": "Built enterprise-grade Spring Boot integrations"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename Certifications to Technologies and clean up fake claims</name>
  <files>locales/en/trustSignals.json, locales/es/trustSignals.json, components/sections/TrustSignals.tsx</files>
  <action>
  **locales/en/trustSignals.json:**
  1. Change `"certificationsTitle"` value from `"Certifications & Technologies"` to `"Technologies"`
  2. Replace the `"certifications"` array with honest technology names -- no "Certified", "Expert", or "Specialist" claims:
     ```json
     "certifications": [
       "React",
       "Next.js",
       "Node.js",
       "TypeScript",
       "Spring Boot",
       "Docker & K8s",
       "PostgreSQL",
       "AWS"
     ]
     ```
     Note: The JSON key stays `"certifications"` to avoid breaking the component's `t.raw('certifications')` call. Only the displayed title changes.

  **locales/es/trustSignals.json:**
  1. Change `"certificationsTitle"` value from `"Certificaciones y Tecnologias"` to `"Tecnologias"` (match accent pattern already used in file)
  2. Replace the `"certifications"` array identically:
     ```json
     "certifications": [
       "React",
       "Next.js",
       "Node.js",
       "TypeScript",
       "Spring Boot",
       "Docker y K8s",
       "PostgreSQL",
       "AWS"
     ]
     ```

  **components/sections/TrustSignals.tsx:**
  1. Rename the `CertBadge` component to `TechBadge` (and update the JSX reference on line ~170)
  2. Remove the `CheckCircle` import from lucide-react (line 4) since it will no longer be used
  3. In the renamed `TechBadge` component, remove the `<CheckCircle>` icon entirely. The badge should just show the technology name text without any certification-implying icon. Keep the existing styling classes.
  4. Update the comment on line 158 from "Certifications & Skills Badges" to "Technologies"
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - EN trustSignals.json title reads "Technologies", no "Certified/Expert/Specialist" strings in certifications array
  - ES trustSignals.json title reads "Tecnologias" (with appropriate accents), matching cleaned array
  - TrustSignals.tsx no longer imports CheckCircle
  - TrustSignals.tsx badge component renders technology name only, no certification icon
  - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Spring Boot-SageConnect association and audit skill accuracy</name>
  <files>locales/en/skills.json, locales/es/skills.json, locales/en/timeline.json, locales/es/timeline.json</files>
  <action>
  **Spring Boot project reference fix in skills.json (both EN and ES):**

  In `locales/en/skills.json`, find the Spring Boot entry in "Frameworks & Libraries" category (currently line 65-69):
  ```json
  {
    "name": "Spring Boot",
    "experience": "3 years",
    "level": 85,
    "project": "SageConnect ERP integration platform"
  }
  ```
  Change the `"project"` value to: `"Enterprise Java backend services and REST APIs"`

  In `locales/es/skills.json`, find the matching Spring Boot entry and change `"project"` from `"SageConnect plataforma de integracion ERP"` to: `"Servicios backend Java empresariales y APIs REST"`

  **Timeline Spring Boot fix (both EN and ES):**

  In `locales/en/timeline.json`, find `roles.sageconnect.description2` (currently `"Built enterprise-grade Spring Boot integrations"`).
  Change to: `"Built Node.js/Express integrations with Sage 300 Web API"`

  In `locales/es/timeline.json`, find matching `roles.sageconnect.description2` (currently `"Construi integraciones Spring Boot de grado empresarial"`).
  Change to: `"Construi integraciones Node.js/Express con Sage 300 Web API"` (match accent pattern already in file)

  **Skill accuracy audit -- apply these corrections:**

  The skills.json files currently show some inflated experience years and levels. Apply conservative corrections:

  In BOTH `locales/en/skills.json` and `locales/es/skills.json`:

  1. Spring Boot: Change `"experience"` from `"3 years"` / `"3 anos"` to `"2 years"` / `"2 anos"` and `"level"` from 85 to 75 (Spring Boot is known but not SageConnect-level usage)
  2. Java: Change `"experience"` from `"3 years"` / `"3 anos"` to `"2 years"` / `"2 anos"` (Spring Boot was 2 years, Java aligns). Change the `"project"` field:
     - EN: from `"Enterprise-grade Spring Boot applications"` to `"Backend services and enterprise applications"`
     - ES: from `"Aplicaciones Spring Boot de grado empresarial"` to `"Servicios backend y aplicaciones empresariales"`

  All other skill entries remain unchanged -- they reference real projects and have reasonable numbers.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - `grep -r "Spring Boot" locales/en/skills.json` returns the entry but with project NOT mentioning SageConnect
  - `grep -r "Spring Boot" locales/en/timeline.json` returns zero results
  - `grep -r "Spring Boot" locales/es/timeline.json` returns zero results
  - EN and ES skills.json have identical structure (same keys, same skill count, same categories)
  - EN and ES timeline.json SageConnect entries both reference Node.js/Express
  - Java skill entry no longer mentions Spring Boot in its project description
  - Spring Boot experience is "2 years" with level 75 in both locale files
  - Build passes
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -ri "certified\|expert\|specialist" locales/en/trustSignals.json locales/es/trustSignals.json` returns nothing
- `grep -i "certificat" locales/en/trustSignals.json` -- only the JSON key name `certifications`, NOT the displayed title
- `grep "CheckCircle" components/sections/TrustSignals.tsx` returns nothing
- `grep -i "spring boot" locales/en/timeline.json locales/es/timeline.json` returns nothing
- `grep -i "SageConnect" locales/en/skills.json locales/es/skills.json` returns nothing (Spring Boot project field no longer says SageConnect)
- Spring Boot still appears in the skills list (just with corrected project reference)
</verification>

<success_criteria>
The "Certifications & Technologies" section now reads just "Technologies" with no fake certification claims. Spring Boot appears in the general skills list but with no SageConnect association. The SageConnect timeline correctly references JavaScript/Node.js/Express. All changes are consistent across EN and ES locale files.
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-trust-signals/07-02-SUMMARY.md`
</output>
