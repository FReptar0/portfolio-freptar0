---
phase: 02-new-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[locale]/projects/[slug]/page.tsx
  - app/[locale]/projects/[slug]/tech/page.tsx
  - components/sections/Projects.tsx
  - locales/en/caseStudy.json
  - locales/es/caseStudy.json
  - locales/en/projects.json
  - locales/es/projects.json
autonomous: true
requirements: [CS-01, CS-02, CS-03, CS-04, CS-05]

must_haves:
  truths:
    - "Navigating to /en/projects/sagesync renders the SageSync case study page (not 404)"
    - "Navigating to /en/projects/cardeal renders the Qardeal case study page (not 404)"
    - "Navigating to /en/projects/gymmanager renders the Gym Manager case study page (not 404)"
    - "Navigating to /en/projects/sagesync/tech renders the SageSync tech deep-dive page (not 404)"
    - "Navigating to /en/projects/cardeal/tech renders the Qardeal tech deep-dive page (not 404)"
    - "Navigating to /en/projects/gymmanager/tech renders the Gym Manager tech deep-dive page (not 404)"
    - "The Spanish equivalents (/es/projects/[slug] and /es/projects/[slug]/tech) render fully translated content"
    - "Each new case study page has a unique title and meta description derived from its slug"
    - "The Qardeal metric shows 45% (not 35%) in both metric card and narrative prose, in both locales"
    - "Homepage Projects section shows View Case Study link for sagesync, cardeal, and gymmanager"
  artifacts:
    - path: "app/[locale]/projects/[slug]/page.tsx"
      provides: "Case study routing for all 4 slugs"
      contains: "'sagesync', 'cardeal', 'gymmanager'"
    - path: "app/[locale]/projects/[slug]/tech/page.tsx"
      provides: "Tech deep-dive routing for all 4 slugs"
      contains: "'sagesync', 'cardeal', 'gymmanager'"
    - path: "components/sections/Projects.tsx"
      provides: "Homepage View Case Study links for all 4 projects"
      contains: "'sagesync', 'cardeal', 'gymmanager'"
    - path: "locales/en/caseStudy.json"
      provides: "Corrected Qardeal metric (45%)"
      contains: "45%"
    - path: "locales/es/caseStudy.json"
      provides: "Corrected Qardeal metric (45%) in Spanish"
      contains: "45%"
  key_links:
    - from: "app/[locale]/projects/[slug]/page.tsx"
      to: "locales/*/caseStudy.json"
      via: "VALID_SLUGS drives generateStaticParams and notFound guard"
      pattern: "VALID_SLUGS.*sagesync.*cardeal.*gymmanager"
    - from: "app/[locale]/projects/[slug]/tech/page.tsx"
      to: "components/case-study/TechDeepDive.tsx"
      via: "VALID_SLUGS enables slug to reach TechDeepDive which maps slug to diagram"
      pattern: "VALID_SLUGS.*sagesync.*cardeal.*gymmanager"
    - from: "components/sections/Projects.tsx"
      to: "app/[locale]/projects/[slug]/page.tsx"
      via: "projectsWithCaseStudy Set controls View Case Study link visibility"
      pattern: "projectsWithCaseStudy.*sagesync.*cardeal.*gymmanager"
---

<objective>
Activate SageSync, Qardeal, and Gym Manager as live case study pages by wiring their slugs into the existing routing system, fixing the Qardeal metric accuracy, and enabling homepage discovery links.

Purpose: All content, components, and diagrams already exist from Phase 1. This plan flips the switch by adding three slugs to two VALID_SLUGS arrays (main page + tech page), adding three slugs to the homepage projectsWithCaseStudy Set, and correcting the Qardeal booking conversion metric from 35% to 45% per user decision.

Output: Six new static routes (3 slugs x 2 pages) in both locales (12 total new pages), three new homepage View Case Study links, and accurate Qardeal metrics across all locale files.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-new-case-studies/02-CONTEXT.md
@.planning/phases/02-new-case-studies/02-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From app/[locale]/projects/[slug]/page.tsx (line 13):
```typescript
const VALID_SLUGS = ['sageconnect'] as const;
```
Target:
```typescript
const VALID_SLUGS = ['sageconnect', 'sagesync', 'cardeal', 'gymmanager'] as const;
```

From app/[locale]/projects/[slug]/tech/page.tsx (line 12):
```typescript
const VALID_SLUGS = ['sageconnect'] as const;
```
Target:
```typescript
const VALID_SLUGS = ['sageconnect', 'sagesync', 'cardeal', 'gymmanager'] as const;
```

From components/sections/Projects.tsx (line 34):
```typescript
const projectsWithCaseStudy = new Set(['sageconnect']);
```
Target:
```typescript
const projectsWithCaseStudy = new Set(['sageconnect', 'sagesync', 'cardeal', 'gymmanager']);
```

From locales/en/caseStudy.json (lines 202, 208) — Qardeal metric locations:
```json
// Line 202 (impact prose paragraph):
"The platform launched in 4 months, immediately capturing online bookings. Conversion rates improved by 35% compared to the phone-based process, with 99.2% payment success rate."
// Line 208 (metric value):
{ "value": "35%", "label": "More Bookings" }
```

From locales/es/caseStudy.json (lines 202, 208) — same structure:
```json
// Line 202 (impact prose paragraph):
"La plataforma se lanzó en 4 meses, capturando reservas en línea inmediatamente. Las tasas de conversión mejoraron un 35% comparado con el proceso telefónico, con 99.2% de éxito en transacciones de pago."
// Line 208 (metric value):
{ "value": "35%", "label": "Más Reservas" }
```

From locales/en/projects.json (line 146) — homepage metric:
```json
{ "label": "Booking Conversion", "value": "35% increase" }
```

From locales/es/projects.json (line 146) — homepage metric:
```json
{ "label": "Conversión de Reservas", "value": "35% aumento" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Qardeal metric from 35% to 45% in all locale files</name>
  <files>locales/en/caseStudy.json, locales/es/caseStudy.json, locales/en/projects.json, locales/es/projects.json</files>
  <action>
Update the Qardeal booking conversion metric from "35%" to "45%" in all four locale files per user decision. There are 6 occurrences total:

1. `locales/en/caseStudy.json` line ~208: Change metric value `"35%"` to `"45%"` in `projects.cardeal.metrics[1].value`
2. `locales/en/caseStudy.json` line ~202: Change prose text `"Conversion rates improved by 35%"` to `"Conversion rates improved by 45%"` in `projects.cardeal.impact.paragraphs[0]`
3. `locales/es/caseStudy.json` line ~208: Change metric value `"35%"` to `"45%"` in `projects.cardeal.metrics[1].value`
4. `locales/es/caseStudy.json` line ~202: Change prose text `"mejoraron un 35%"` to `"mejoraron un 45%"` in `projects.cardeal.impact.paragraphs[0]`
5. `locales/en/projects.json` line ~146: Change `"35% increase"` to `"45% increase"` in `items.cardeal.businessImpact.metric1.value`
6. `locales/es/projects.json` line ~146: Change `"35% aumento"` to `"45% aumento"` in `items.cardeal.businessImpact.metric1.value`

Do NOT change any other metric values. Only Qardeal's booking conversion / "More Bookings" / "Mas Reservas" metric changes. Verify after editing by searching all locale files for "35%" — there should be zero matches.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && grep -r "35%" locales/ && echo "FAIL: 35% still found" || echo "PASS: no 35% references remain"</automated>
  </verify>
  <done>All 6 occurrences of "35%" in Qardeal content changed to "45%" across all 4 locale files. No other metrics altered. Grep confirms zero remaining "35%" references in locales/.</done>
</task>

<task type="auto">
  <name>Task 2: Register three new slugs in routing and homepage discovery</name>
  <files>app/[locale]/projects/[slug]/page.tsx, app/[locale]/projects/[slug]/tech/page.tsx, components/sections/Projects.tsx</files>
  <action>
Add 'sagesync', 'cardeal', and 'gymmanager' to three locations:

1. `app/[locale]/projects/[slug]/page.tsx` line 13: Change `const VALID_SLUGS = ['sageconnect'] as const;` to `const VALID_SLUGS = ['sageconnect', 'sagesync', 'cardeal', 'gymmanager'] as const;`

2. `app/[locale]/projects/[slug]/tech/page.tsx` line 12: Change `const VALID_SLUGS = ['sageconnect'] as const;` to `const VALID_SLUGS = ['sageconnect', 'sagesync', 'cardeal', 'gymmanager'] as const;`

3. `components/sections/Projects.tsx` line 34: Change `const projectsWithCaseStudy = new Set(['sageconnect']);` to `const projectsWithCaseStudy = new Set(['sageconnect', 'sagesync', 'cardeal', 'gymmanager']);`

IMPORTANT: Keep the `as const` assertion on VALID_SLUGS. Keep the existing type cast in the `notFound()` guard (`slug as (typeof VALID_SLUGS)[number]`) — do not modify the guard logic. Do NOT add any new imports, components, or files. These are the only three lines that change.

After editing, run `npm run build` to verify all 24 static pages generate successfully (4 slugs x 2 pages x 2 locales + existing pages). The build will fail if any translation key is missing or any slug is misconfigured.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -40</automated>
  </verify>
  <done>Build succeeds with all new static routes generated. `npm run build` output shows pages for /en/projects/sagesync, /en/projects/cardeal, /en/projects/gymmanager and their /tech counterparts plus Spanish equivalents. Homepage Projects section will show "View Case Study" links for all four projects.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — confirms all 24 case study static pages generate without errors (SSG catches missing translation keys, bad slugs, component errors)
2. `npm run lint` passes — no TypeScript or ESLint issues introduced
3. `grep -r "35%" locales/` returns zero matches — Qardeal metric fully corrected
4. Verify VALID_SLUGS contains exactly 4 entries in both page.tsx and tech/page.tsx
5. Verify projectsWithCaseStudy Set contains exactly 4 entries in Projects.tsx
</verification>

<success_criteria>
- All six new routes (/projects/sagesync, /projects/cardeal, /projects/gymmanager + their /tech pages) render in both EN and ES locales (verified by successful SSG build)
- Each new case study page has unique SEO metadata (title from headline, description from tagline — verified by build completing generateMetadata without errors)
- Qardeal metric shows "45%" everywhere (metric cards + narrative prose + homepage cards, both locales)
- Homepage shows "View Case Study" link for SageSync, Qardeal, and Gym Manager in addition to SageConnect
- `npm run build && npm run lint` both pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-new-case-studies/02-01-SUMMARY.md`
</output>
