---
phase: 01-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/case-study/CaseStudyHero.tsx
  - components/case-study/CaseStudyNarrative.tsx
  - components/case-study/MetricsGrid.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "Hero section has a visible gradient mesh background behind headline and tags"
    - "Role badge, tags, and headline are visually distinct from body text — not a wall of paragraphs"
    - "Narrative sections (Overview, Problem, Solution, Impact) have visual separation between the Overview card and the Problem/Solution/Impact block"
    - "Metric callout cards have gradient backgrounds and large gradient text that draws the eye"
  artifacts:
    - path: "components/case-study/CaseStudyHero.tsx"
      provides: "Hero with gradient mesh background and improved hierarchy"
      contains: "apple-gradient-mesh"
    - path: "components/case-study/CaseStudyNarrative.tsx"
      provides: "Narrative with section separator between Overview and Problem/Solution/Impact"
      contains: "border-t"
    - path: "components/case-study/MetricsGrid.tsx"
      provides: "Metric cards with gradient background and gradient text"
      contains: "bg-gradient-to-br"
  key_links:
    - from: "components/case-study/CaseStudyHero.tsx"
      to: "app/globals.css"
      via: "apple-gradient-mesh CSS class"
      pattern: "apple-gradient-mesh"
---

<objective>
Polish the SageConnect case study page's visual hierarchy — add a gradient background to the hero, improve text hierarchy, add section separators between narrative blocks, and verify metric callout prominence.

Purpose: A recruiter viewing `/projects/sageconnect` should immediately see polished visual hierarchy — the hero has weight, sections breathe, and metrics pop.
Output: Updated CaseStudyHero.tsx, CaseStudyNarrative.tsx, and MetricsGrid.tsx with visual refinements.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation/01-RESEARCH.md

@components/case-study/CaseStudyHero.tsx
@components/case-study/CaseStudyNarrative.tsx
@components/case-study/MetricsGrid.tsx
@app/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/case-study/CaseStudyHero.tsx:
```typescript
interface CaseStudyHeroProps {
  slug: string;
  locale: string;
}
export default function CaseStudyHero({ slug, locale }: CaseStudyHeroProps)
```

From components/case-study/CaseStudyNarrative.tsx:
```typescript
interface CaseStudyNarrativeProps {
  slug: string;
}
export default function CaseStudyNarrative({ slug }: CaseStudyNarrativeProps)
```

From components/case-study/MetricsGrid.tsx:
```typescript
interface MetricsGridProps {
  metrics: Array<{ value: string; label: string }>;
}
export default function MetricsGrid({ metrics }: MetricsGridProps)
```

CSS utilities available in globals.css:
- `apple-gradient-mesh` — radial gradient mesh background (dark mode aware)
- `apple-glass` — glass morphism card with backdrop-filter
- `bg-gradient-to-r from-primary-blue to-accent-purple bg-clip-text text-transparent` — gradient text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gradient mesh background to CaseStudyHero and improve visual hierarchy</name>
  <files>components/case-study/CaseStudyHero.tsx</files>
  <action>
Edit `CaseStudyHero.tsx` to add a gradient mesh background and strengthen visual hierarchy:

1. **Hero background (VIS-03):** Add an absolute-positioned `apple-gradient-mesh` background div inside the `<section>`. The section needs `overflow-hidden` to contain it. Wrap all existing content in a `relative z-10` container div so it sits above the background layer.

Before:
```tsx
<section className="pt-32 pb-16 relative">
  <div className="container mx-auto px-6 lg:px-8 max-w-5xl">
```

After:
```tsx
<section className="pt-32 pb-16 relative overflow-hidden">
  <div className="absolute inset-0 apple-gradient-mesh pointer-events-none" aria-hidden="true" />
  <div className="relative z-10 container mx-auto px-6 lg:px-8 max-w-5xl">
```

Make sure to close the new wrapper div correctly at the end.

2. **Headline hierarchy (VIS-01):** Bump the headline from `text-4xl md:text-6xl` to `text-5xl md:text-7xl` for more visual weight on the hero.

3. **Role badge emphasis (VIS-01):** Change the role badge from `text-sm` to `text-base font-bold` so it reads more prominently. The `bg-primary-blue/20 rounded-full` background stays.

4. **"Team of" label:** The "Team of {teamSize}" text is hardcoded in English. This is not in scope for this phase, but do NOT change it — it will be addressed when i18n cleanup happens. Leave as-is.

IMPORTANT: Do NOT modify the component's props interface. Do NOT add "use client" — this component does not have it and may be imported into server context. Do NOT remove the ArrowLeft back link. Preserve all existing functionality.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CaseStudyHero has a visible gradient mesh background behind content, headline is larger (text-5xl/text-7xl), and role badge uses text-base font-bold. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add section separators to CaseStudyNarrative and verify MetricsGrid prominence</name>
  <files>components/case-study/CaseStudyNarrative.tsx, components/case-study/MetricsGrid.tsx</files>
  <action>
**CaseStudyNarrative.tsx — Section separators (VIS-02):**

Add a thin horizontal separator between the Overview card and the Problem/Solution/Impact block. Insert this line after the closing `</div>` of the Overview card (the `apple-glass rounded-3xl p-8 md:p-12 mb-12` div) and before the `<div className="space-y-8">` that contains the sections array:

```tsx
{/* Section separator */}
<div className="my-8 border-t border-foreground/[0.06]" aria-hidden="true" />
```

Also change the Overview card's bottom margin from `mb-12` to `mb-0` since the separator now handles the visual gap with its own `my-8`.

This creates a subtle visual break between the "what this project is" (Overview) and the "how it went" (Problem/Solution/Impact) narrative blocks. Do NOT add separators between Problem, Solution, and Impact — they already have `space-y-8` and colored left borders that differentiate them.

**MetricsGrid.tsx — Verify prominence (VIS-04):**

The MetricsGrid already has gradient backgrounds (`bg-gradient-to-br from-primary-blue/10 to-accent-purple/10`) and gradient text (`text-3xl md:text-4xl font-bold bg-gradient-to-r from-primary-blue to-accent-purple bg-clip-text text-transparent`). This is substantially complete.

Make one minor enhancement: add a section heading above the metrics grid so recruiters know what these numbers represent. Add an `<h2>` before the grid:

```tsx
<h2 className="text-2xl font-bold mb-6">Key Metrics</h2>
```

Wait — MetricsGrid receives metrics as props and has no access to translations. Do NOT add a heading here; the heading is the responsibility of the page that composes MetricsGrid. Leave MetricsGrid unchanged unless you find the gradient text or card styling needs adjustment for contrast. If the existing styling looks solid after the build, leave it as-is.

Actually, confirm MetricsGrid is fine as-is. The research says "Considered substantially complete." No changes needed to MetricsGrid.tsx.

IMPORTANT: Do NOT change the component interfaces. Do NOT add any new dependencies. Preserve the "What's Next" try/catch pattern in CaseStudyNarrative.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>CaseStudyNarrative has a subtle border-t separator between the Overview card and the Problem/Solution/Impact block. MetricsGrid remains unchanged (already has gradient treatment). Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npm run lint` passes
3. CaseStudyHero.tsx contains `apple-gradient-mesh` class
4. CaseStudyNarrative.tsx contains a `border-t border-foreground` separator element
5. No TypeScript errors in modified files
</verification>

<success_criteria>
- Build succeeds with all visual changes
- Hero section has gradient mesh background visible in the DOM (absolute div with apple-gradient-mesh)
- Headline uses text-5xl/text-7xl sizing
- Role badge uses text-base font-bold
- Narrative has a thin separator between Overview and Problem/Solution/Impact
- MetricsGrid gradient treatment is preserved (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-01-SUMMARY.md`
</output>
