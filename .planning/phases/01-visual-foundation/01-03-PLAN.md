---
phase: 01-visual-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
  - VIS-05
  - ARCH-01

must_haves:
  truths:
    - "All six visual/architecture requirements are verified by human inspection on desktop and mobile viewports"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 1 changes on desktop and 375px mobile emulation.

Purpose: All Phase 1 requirements are visual — automated builds confirm no crashes but cannot confirm the page looks right. This checkpoint verifies the recruiter-facing experience.
Output: User approval or specific issues to fix.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and lint to confirm all changes compile</name>
  <files></files>
  <action>
Run `npm run build && npm run lint` to verify all changes from Plans 01 and 02 compile cleanly. Start the dev server with `npm run dev` so the user can inspect the page.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -10 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>Build and lint pass with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 1 requirements</name>
  <files></files>
  <action>
Present the user with verification instructions for all six Phase 1 requirements. The dev server should already be running from Task 1. Guide the user through each requirement check.

What was built across Plans 01 and 02:
- VIS-01: Hero headline bumped to text-5xl/text-7xl, role badge uses text-base font-bold
- VIS-02: Thin separator line between Overview card and Problem/Solution/Impact block
- VIS-03: Gradient mesh background behind hero section
- VIS-04: Metric cards with gradient backgrounds and gradient text (pre-existing, verified)
- VIS-05: Diagram scrolls horizontally on mobile with min-width 640px
- ARCH-01: "CFDI Importer" replaced with "Sage 300 Web API" node (dashed border, "Planned Migration" label)

Verification steps for the user:
1. Open http://localhost:3000/en/projects/sageconnect in your browser
2. VIS-03 (Hero background): Confirm a subtle gradient mesh is visible behind the hero headline and tags area
3. VIS-01 (Hierarchy): Confirm the headline text is large and prominent, the role badge stands out with bold text, tags are visually distinct
4. VIS-02 (Section separation): Scroll to the narrative sections — confirm there is a thin line between the Overview card and the Problem/Solution/Impact cards
5. VIS-04 (Metrics): Scroll to the metrics section — confirm cards have gradient backgrounds and large gradient text numbers
6. Navigate to http://localhost:3000/en/projects/sageconnect/tech
7. ARCH-01 (Diagram): Confirm the bottom node says "Sage 300 Web API" (not "CFDI Importer"), has a dashed border, shows "Planned Migration" in accent color, and the arrow label reads "REST API (Planned)"
8. VIS-05 (Mobile): Open Chrome DevTools (F12), toggle device toolbar (Ctrl+Shift+M), select iPhone SE or set width to 375px — confirm the diagram is horizontally scrollable and text labels are readable
9. Check both pages in dark mode (toggle theme) — confirm gradient mesh and diagram colors work in dark mode

Resume signal: Type "approved" if everything looks good, or describe specific issues.
  </action>
  <verify>
    <automated>echo "Checkpoint — requires human visual verification"</automated>
  </verify>
  <done>User confirms all six requirements are visually met by typing "approved", or provides specific feedback for gap closure.</done>
</task>

</tasks>

<verification>
User confirms all six requirements are visually met.
</verification>

<success_criteria>
- User types "approved" — all visual requirements confirmed
- OR user provides specific feedback items that can be addressed in a gap closure plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation/01-03-SUMMARY.md`
</output>
