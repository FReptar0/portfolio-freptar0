---
phase: 06-motion-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - components/sections/Hero.tsx
  - components/sections/Projects.tsx
  - components/sections/CareerTimeline.tsx
  - components/sections/Process.tsx
  - components/sections/Contact.tsx
  - components/ui/Navigation.tsx
autonomous: false
requirements: [MOTN-02, PLSH-04]

must_haves:
  truths:
    - "Different component types have visually distinct hover/interaction patterns"
    - "Buttons darken background and scale(1.02) on hover, scale(0.98) on active/press"
    - "Nav links have animated underline width on hover (already implemented -- verify preserved)"
    - "Project accordion rows have smooth height transition on expand/collapse"
    - "Timeline entries show accent border on left side when hovered"
    - "Metric values change color intensity on hover"
    - "No uniform translateY(-2px) + border-color pattern on everything"
    - "All existing functionality works: i18n, case studies, contact form, SEO, navigation"
    - "Build produces all 33 pages successfully"
  artifacts:
    - path: "components/sections/Projects.tsx"
      provides: "Smooth accordion expand/collapse with CSS height transition"
      contains: "transition"
    - path: "components/sections/CareerTimeline.tsx"
      provides: "Left border accent on hover for timeline entries"
      contains: "hover"
  key_links:
    - from: "components/sections/Projects.tsx"
      to: "accordion expand state"
      via: "CSS max-height or grid-template-rows transition"
      pattern: "transition.*height\\|grid-template-rows"
    - from: "components/ui/Navigation.tsx"
      to: "nav links"
      via: "underline width animation on hover"
      pattern: "group-hover:w-full"
---

<objective>
Add varied, component-specific hover and interaction patterns across the site, then run a comprehensive regression verification to confirm all existing functionality is preserved.

Purpose: Ensure the portfolio has distinct, intentional interaction patterns per component type (not uniform hover effects everywhere) and verify the entire v2.0 Swiss Precision Redesign is complete and functional.
Output: Varied hover effects across components + passing full regression check confirming all 33 pages, i18n, case studies, contact form, and SEO metadata work correctly.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-motion-accessibility/06-01-SUMMARY.md

@components/sections/Hero.tsx
@components/sections/Projects.tsx
@components/sections/CareerTimeline.tsx
@components/sections/Process.tsx
@components/sections/Contact.tsx
@components/ui/Navigation.tsx

<interfaces>
<!-- From Phase 5 decisions: -->
<!-- "Replaced hover:scale effects with hover:border-accent for Swiss restrained interaction" -->
<!-- This plan adds BACK selective, varied hover effects per MOTN-02 requirements -->
<!-- Cards keep border-accent hover. Buttons get scale. Timeline gets border-left. Metrics get color. -->

<!-- Navigation.tsx line 111: already has underline animation -->
<!-- <span className="absolute bottom-0 left-0 w-0 h-0.5 group-hover:w-full transition-all duration-300" style={{ background: 'var(--accent-500)' }} /> -->

<!-- Projects.tsx line 337: Expanded detail currently uses conditional render {isExpanded && (...)} -->
<!-- This needs to change to always-rendered with CSS height transition for smooth animation -->

<!-- Hero.tsx CTAs (line 60-79): Primary button uses inline onMouseEnter/Leave for bg color -->
<!-- These should additionally get scale(1.02) on hover and scale(0.98) on active -->

<!-- Contact.tsx submit button (line 591-625): Already has active:scale-[0.98] -->
<!-- Needs hover:scale-[1.02] added -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add varied hover and interaction patterns across components</name>
  <files>components/sections/Hero.tsx, components/sections/Projects.tsx, components/sections/CareerTimeline.tsx, components/sections/Process.tsx, components/sections/Contact.tsx, components/ui/Navigation.tsx</files>
  <action>
**MOTN-02 Requirement:** Each component type should have a DISTINCT hover pattern. No uniform `translateY(-2px) + border-color` on everything.

**Pattern mapping:**
- **Cards:** Keep existing `hover:border-[var(--color-accent)]` (from Phase 5) -- this is their pattern
- **Buttons:** `hover:scale-[1.02]` + background darken, `active:scale-[0.98]` on press
- **Nav links:** Underline width animation (already done -- verify preserved)
- **Accordion:** Smooth height transition on expand/collapse
- **Timeline entries:** Border-left accent color on hover
- **Metric values:** Color brightness/intensity change on hover

**Hero.tsx -- Button hover effects:**

1. Primary CTA button (the green "View Projects" button, line 62-68): Add `hover:scale-[1.02] active:scale-[0.98]` to className. Keep existing `transition-colors duration-300` but change to `transition-all duration-200` to cover the transform.
2. Secondary CTA button (the bordered "Contact" button, line 69-78): Add `hover:scale-[1.02] active:scale-[0.98]` to className. Change `transition-colors` to `transition-all duration-200`.
3. MetricItem component: Add `group` class to the wrapper div, and on the value div add `group-hover:brightness-125 transition-all duration-200` or more precisely, on hover change the accent color to the hover variant: add `hover:text-[var(--color-accent-hover)]` alongside the existing accent color styling.

**Projects.tsx -- Smooth accordion expand/collapse:**

1. Replace the conditional render `{isExpanded && (...)}` pattern with an always-rendered div that uses CSS transition for smooth height animation.
2. Approach: Wrap the expanded detail in a div with `overflow-hidden` and use `grid` + `grid-template-rows` trick:
   ```
   <div className={`grid transition-[grid-template-rows] duration-300 ease-out ${isExpanded ? 'grid-rows-[1fr]' : 'grid-rows-[0fr]'}`}>
     <div className="overflow-hidden">
       {/* existing expanded content */}
     </div>
   </div>
   ```
   This gives a smooth height transition from 0 to auto without needing a fixed height.
3. The `pt-6 pb-8` padding on the inner content should be part of the overflow-hidden div so it collapses cleanly.
4. Also add hover effect to collapsed project rows: the button row should get a subtle background tint on hover: `hover:bg-[var(--color-surface-elevated)]` to indicate interactivity.

**CareerTimeline.tsx -- Border-left accent on hover:**

1. Each `TimelineEntry` div should get a left border that activates on hover:
   - Add to the entry wrapper: `border-l-2 border-transparent hover:border-[var(--color-accent)] transition-colors duration-200`
   - This adds a colored left border on hover that complements the vertical timeline line
   - Adjust padding-left if needed to accommodate the border without shifting content

**Process.tsx -- Button hover effect on CTA:**

1. The bottom CTA `<a>` tag (line 116-119) already has `hover:bg-[var(--color-accent-hover)]`. Add `hover:scale-[1.02] active:scale-[0.98]` and change `transition-all duration-300` to `transition-all duration-200`.
2. Keep existing `hover:border-[var(--color-accent)]` on ProcessCard and ProcessCardMobile -- cards use border hover.

**Contact.tsx -- Button hover effect:**

1. Submit button (line 591-594): Already has `active:scale-[0.98]`. Add `hover:scale-[1.02]` to the className. The disabled state already has `disabled:scale-100` which will override.
2. Quick action link cards (LinkedIn, GitHub, Calendar) already have `hover:border-[var(--color-accent)]` -- keep this pattern for cards.

**Navigation.tsx -- Verify underline animation preserved:**

1. Verify that the nav link underline animation (line 111: `w-0 h-0.5 group-hover:w-full transition-all duration-300`) is still present and working.
2. No changes needed if it exists. Just verify during review.
3. Additionally, add `hover:scale-[1.02] active:scale-[0.98]` to the "Hire Me" CTA button (line 117-125) alongside existing hover background change. Change the existing `transition-colors duration-300` attribute approach to include transform -- add `transition-all duration-200` to className.

**Summary of distinct patterns after this task:**
| Component Type | Hover Effect |
|----------------|-------------|
| Cards (TrustSignals, Process, Contact actions) | border-color change to accent |
| Buttons (Hero CTAs, Process CTA, Contact submit, Nav Hire Me) | scale(1.02) + bg darken, active scale(0.98) |
| Nav links | Underline width animation 0->full |
| Accordion rows (Projects) | Subtle bg tint + smooth height transition |
| Timeline entries | Left border accent color |
| Metric values (Hero) | Color intensity/brightness change on hover |
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Hero buttons scale(1.02) on hover, scale(0.98) on active
    - Hero metric values change color on hover
    - Projects accordion expands/collapses with smooth CSS height transition (no conditional render jump)
    - CareerTimeline entries show accent left border on hover
    - Process CTA button has scale hover effect
    - Contact submit button has hover:scale-[1.02]
    - Nav links preserve underline width animation
    - Nav "Hire Me" button has scale hover effect
    - At least 4 distinct hover patterns exist across the site
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full regression verification and visual confirmation</name>
  <what-built>
    Complete Phase 6 Motion and Accessibility implementation:
    1. Scroll-triggered entrance animations on all 8 homepage sections (via Intersection Observer)
    2. Varied hover patterns: button scale, nav underline, accordion smooth height, timeline border-left, metric color intensity
    3. Hero entrance reduced to 300ms, status badge pulse removed (static dot)
    4. prefers-reduced-motion support in both CSS and hook
  </what-built>
  <how-to-verify>
    **Full regression verification (PLSH-04):**

    1. **Build check:** Run `npm run build` -- must complete with no errors, all 33 pages generated
    2. **Start dev server:** Run `npm run dev` and open http://localhost:3000

    **Scroll animations (MOTN-01):**
    3. Scroll slowly from top to bottom of homepage -- each section should fade/slide in as it enters viewport
    4. Sections should stagger their children (metrics, cards, timeline entries appear sequentially)

    **Varied hover effects (MOTN-02):**
    5. Hover over Hero CTA buttons -- should scale up slightly
    6. Hover over nav links -- underline should animate width
    7. Click different project accordion rows -- should expand/collapse smoothly (no jump)
    8. Hover over timeline entries -- left border accent should appear
    9. Hover over Hero metric values -- color should intensify

    **Anti-patterns fixed (MOTN-03):**
    10. Hero section appears quickly (300ms, not slow 1s fade)
    11. Status badge is a static green dot (no pulsing/pinging)
    12. No bounce scroll indicator anywhere

    **Reduced motion (MOTN-04):**
    13. In browser DevTools, enable "prefers-reduced-motion: reduce" (Chrome: Rendering tab > Emulate CSS media feature)
    14. Reload page -- all content should appear instantly, no animations

    **i18n (PLSH-04):**
    15. Toggle language to English -- all content translates correctly
    16. Toggle back to Spanish -- all content correct
    17. Navigate to /en and /es directly -- both work

    **Case studies (PLSH-04):**
    18. Visit /es/projects/sageconnect -- case study loads
    19. Visit /en/projects/sagesync -- case study loads
    20. Visit /es/projects -- projects index loads

    **Contact form (PLSH-04):**
    21. Navigate to contact section -- form renders with all fields
    22. Form validation works (try submitting empty)

    **SEO (PLSH-04):**
    23. View page source -- verify title, meta description, Open Graph tags present

    **Navigation (PLSH-04):**
    24. From a case study page, click nav links -- they navigate correctly back to homepage sections
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes with 0 errors and generates all 33 pages
2. `npm run lint` passes with no new errors
3. Scroll animations trigger on each section (visual check)
4. At least 4 distinct hover patterns are implemented (button scale, nav underline, accordion height, timeline border, metric color)
5. prefers-reduced-motion disables all animations
6. Both /es and /en routes load correctly
7. Case study routes load: /projects/sageconnect, /projects/sagesync, /projects/cardeal, /projects/gymmanager, /projects/cleany, /projects/carrytrade
8. Contact form renders and validates
9. SEO metadata present in page source
</verification>

<success_criteria>
- Different component types have visually distinct hover/interaction patterns (not uniform)
- Project accordion has smooth CSS height transition (no content jump)
- Timeline entries show accent left border on hover
- Full regression passes: build succeeds, i18n works, case studies load, contact form works, SEO present, navigation works
- Human verification checkpoint confirms all behavior correct
</success_criteria>

<output>
After completion, create `.planning/phases/06-motion-accessibility/06-02-SUMMARY.md`
</output>
