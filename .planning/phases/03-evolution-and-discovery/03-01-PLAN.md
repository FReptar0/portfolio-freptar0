---
phase: 03-evolution-and-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - locales/en/caseStudy.json
  - locales/es/caseStudy.json
  - app/[locale]/projects/[slug]/tech/page.tsx
autonomous: true
requirements:
  - EVO-01
  - EVO-02
  - EVO-03
  - EVO-04

must_haves:
  truths:
    - "SageConnect main case study page shows a purple 'What's Next' glass card describing the exe-to-Web-API migration"
    - "SageSync main case study page shows a 'What's Next' section describing planned evolution"
    - "Tech deep-dive pages for SageConnect and SageSync show the same 'What's Next' section before the CTA"
    - "Projects without whatNext JSON keys (Qardeal, Gym Manager) render no evolution section on either page — no error, no empty block"
    - "Adding whatNext JSON to any future project causes the section to appear without code changes"
  artifacts:
    - path: "locales/en/caseStudy.json"
      provides: "SageSync whatNext content in English"
      contains: "sagesync.*whatNext"
    - path: "locales/es/caseStudy.json"
      provides: "SageSync whatNext content in Spanish"
      contains: "sagesync.*whatNext"
    - path: "app/[locale]/projects/[slug]/tech/page.tsx"
      provides: "whatNext try/catch block on tech deep-dive page"
      contains: "whatNext"
  key_links:
    - from: "components/case-study/CaseStudyNarrative.tsx"
      to: "locales/*/caseStudy.json"
      via: "t(`projects.${slug}.whatNext.title`) try/catch"
      pattern: "whatNext\\.title"
    - from: "app/[locale]/projects/[slug]/tech/page.tsx"
      to: "locales/*/caseStudy.json"
      via: "t(`projects.${slug}.whatNext.title`) try/catch"
      pattern: "whatNext\\.title"
---

<objective>
Add "What's Next" evolution narratives to case studies and tech deep-dive pages.

Purpose: Recruiters see forward-thinking evolution plans on SageConnect and SageSync, demonstrating the developer's ability to think beyond current implementations. The pattern must be extensible via JSON-only additions.

Output: SageSync whatNext content in both locale JSONs, whatNext block on tech deep-dive pages, verified EVO-01/02/03/04 requirements.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evolution-and-discovery/03-CONTEXT.md
@.planning/phases/03-evolution-and-discovery/03-RESEARCH.md

<interfaces>
<!-- Existing whatNext try/catch pattern from CaseStudyNarrative.tsx (lines 72-99) — this is the reference implementation -->

From components/case-study/CaseStudyNarrative.tsx:
```typescript
{/* What's Next - only render if the project has this section */}
{(() => {
  try {
    const whatNextTitle = t(`projects.${slug}.whatNext.title`);
    const whatNextParagraphs = t.raw(`projects.${slug}.whatNext.paragraphs`) as string[];
    return (
      <div className="apple-glass rounded-3xl p-8 md:p-12 border-l-4 border-purple-500/20">
        <h2 className="text-2xl font-bold mb-6 flex items-center gap-3">
          <Rocket className="w-8 h-8 text-purple-500" />
          {whatNextTitle}
        </h2>
        <div className="space-y-4">
          {whatNextParagraphs.map((paragraph, i) => (
            <p key={i} className="text-foreground/80 leading-relaxed">
              {paragraph}
            </p>
          ))}
        </div>
        <div className="mt-6 inline-flex items-center gap-2 px-4 py-2 bg-purple-500/10 rounded-full text-sm font-medium text-purple-600 dark:text-purple-400">
          <span className="w-2 h-2 rounded-full bg-purple-500 animate-pulse" />
          {t('cta.evolution')}
        </div>
      </div>
    );
  } catch {
    return null;
  }
})()}
```

From app/[locale]/projects/[slug]/tech/page.tsx:
```typescript
// Server component — uses getTranslations, NOT useTranslations
const t = await getTranslations({ locale, namespace: 'caseStudy' });
// Imports: Rocket from 'lucide-react' must be added
// whatNext block goes BEFORE the {/* CTA */} section (line 76)
```

From locales/en/caseStudy.json (SageConnect whatNext structure — the template):
```json
"whatNext": {
  "title": "What's Next",
  "paragraphs": [
    "paragraph 1...",
    "paragraph 2...",
    "paragraph 3..."
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SageSync whatNext content and verify EVO-01</name>
  <files>locales/en/caseStudy.json, locales/es/caseStudy.json</files>
  <action>
1. **Verify EVO-01 (SageConnect whatNext already works):** Read `locales/en/caseStudy.json` and confirm `projects.sageconnect.whatNext` exists with `title` and `paragraphs` array. Do the same for `locales/es/caseStudy.json`. EVO-01 requires no code changes — it is already rendering via the existing `CaseStudyNarrative.tsx` try/catch block. Log verification in summary.

2. **Add SageSync whatNext content (EVO-02):** Add a `whatNext` key inside `projects.sagesync` in both locale JSON files. Place it after the last existing key in the sagesync object (likely after `tech` or `tags`), matching the exact structure of sageconnect's whatNext.

**English content (`locales/en/caseStudy.json`):**
```json
"whatNext": {
  "title": "What's Next",
  "paragraphs": [
    "SageSync's next evolution targets a webhook-driven architecture — replacing the current polling-based synchronization with real-time event notifications from both Sage 300 and the e-commerce platform. This shift eliminates the polling interval delay and enables true sub-second sync latency for critical inventory and order data.",
    "The observability roadmap includes containerizing the service with Docker Compose for consistent deployment, and adding a dedicated alert notification channel through email and Slack integration. When dead-letter queue items escalate beyond retry thresholds, operations teams will receive immediate alerts rather than discovering stale sync failures during manual reviews.",
    "These improvements transform SageSync from a reliable batch-oriented bridge into a real-time integration backbone — positioning it to handle higher transaction volumes as the business scales its e-commerce operations."
  ]
}
```

**Spanish content (`locales/es/caseStudy.json`):**
```json
"whatNext": {
  "title": "Proxima Evolucion",
  "paragraphs": [
    "La proxima evolucion de SageSync apunta a una arquitectura basada en webhooks — reemplazando la sincronizacion actual por sondeo con notificaciones de eventos en tiempo real desde Sage 300 y la plataforma de comercio electronico. Este cambio elimina el retraso del intervalo de sondeo y habilita una latencia de sincronizacion inferior al segundo para datos criticos de inventario y pedidos.",
    "La hoja de ruta de observabilidad incluye containerizar el servicio con Docker Compose para despliegues consistentes, y agregar un canal dedicado de notificacion de alertas mediante integracion con correo electronico y Slack. Cuando los elementos de la cola de mensajes fallidos superen los umbrales de reintento, los equipos de operaciones recibiran alertas inmediatas en lugar de descubrir fallos de sincronizacion obsoletos durante revisiones manuales.",
    "Estas mejoras transforman SageSync de un puente confiable orientado a lotes en una columna vertebral de integracion en tiempo real — posicionandolo para manejar mayores volumenes de transacciones a medida que el negocio escala sus operaciones de comercio electronico."
  ]
}
```

IMPORTANT: Maintain valid JSON. The `whatNext` key goes inside the `"sagesync": { ... }` object, at the same level as `headline`, `tagline`, `sections`, etc. Add a comma after the preceding key.

3. **Verify EVO-04 (extensibility by design):** Confirm that no "projectsWithEvolution" Set or slug registration exists anywhere in the codebase: `grep -r "projectsWithEvolution" .` should return empty. The try/catch pattern IS the extensibility mechanism. Log in summary.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && node -e "const en = require('./locales/en/caseStudy.json'); const es = require('./locales/es/caseStudy.json'); const checks = [en.projects.sageconnect.whatNext?.title, en.projects.sageconnect.whatNext?.paragraphs?.length > 0, en.projects.sagesync.whatNext?.title, en.projects.sagesync.whatNext?.paragraphs?.length > 0, es.projects.sageconnect.whatNext?.title, es.projects.sagesync.whatNext?.title]; console.log('All checks:', checks.every(Boolean) ? 'PASS' : 'FAIL', checks);"</automated>
  </verify>
  <done>
    - SageConnect whatNext content exists in both EN and ES locale files (EVO-01 verified)
    - SageSync whatNext content added to both EN and ES locale files with title + paragraphs array (EVO-02 complete)
    - No "projectsWithEvolution" registration exists in codebase (EVO-04 verified by design)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add whatNext block to tech deep-dive page</name>
  <files>app/[locale]/projects/[slug]/tech/page.tsx</files>
  <action>
Add the "What's Next" evolution section to the tech deep-dive page, placed BEFORE the existing CTA section.

1. **Add Rocket import:** Add `Rocket` to the existing lucide-react import on line 10:
   ```typescript
   import { ArrowLeft, ArrowRight, Rocket } from 'lucide-react';
   ```

2. **Add whatNext block:** Insert the following JSX between the `<TechDeepDive slug={slug} />` component (line 74) and the `{/* CTA */}` section (line 76). This is an inline try/catch IIFE — identical pattern to CaseStudyNarrative.tsx but using the server-side `t` from `getTranslations` (already available as `const t` on line 47).

```tsx
{/* What's Next - only render if the project has this section */}
{(() => {
  try {
    const whatNextTitle = t(`projects.${slug}.whatNext.title`);
    const whatNextParagraphs = t.raw(`projects.${slug}.whatNext.paragraphs`) as string[];
    return (
      <section className="py-12">
        <div className="container mx-auto px-6 lg:px-8 max-w-5xl">
          <div className="apple-glass rounded-3xl p-8 md:p-12 border-l-4 border-purple-500/20">
            <h2 className="text-2xl font-bold mb-6 flex items-center gap-3">
              <Rocket className="w-8 h-8 text-purple-500" />
              {whatNextTitle}
            </h2>
            <div className="space-y-4">
              {whatNextParagraphs.map((paragraph: string, i: number) => (
                <p key={i} className="text-foreground/80 leading-relaxed">
                  {paragraph}
                </p>
              ))}
            </div>
            <div className="mt-6 inline-flex items-center gap-2 px-4 py-2 bg-purple-500/10 rounded-full text-sm font-medium text-purple-600 dark:text-purple-400">
              <span className="w-2 h-2 rounded-full bg-purple-500 animate-pulse" />
              {t('cta.evolution')}
            </div>
          </div>
        </div>
      </section>
    );
  } catch {
    return null;
  }
})()}
```

CRITICAL: This is a server component. Use the existing `t` variable from `getTranslations` (line 47). Do NOT import `useTranslations`. The `t.raw()` method works identically in server components.

CRITICAL: The block goes BEFORE the CTA section, not after it. The CTA should remain the last content section on the page.

ANTI-PATTERN: Do NOT extract this into a separate React component. The locked decision requires inline try/catch to maintain the zero-config extensibility story.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Tech deep-dive pages for projects with whatNext content (SageConnect, SageSync) show the purple evolution glass card before the CTA section (EVO-03 complete)
    - Tech deep-dive pages for projects without whatNext content (Qardeal, Gym Manager) render normally with no errors or empty blocks
    - The Rocket icon import is added to the existing lucide-react import line
    - Build passes successfully with all static pages generated
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes — all static pages generated without errors
2. JSON validation: both locale files parse correctly with sagesync.whatNext.title and sagesync.whatNext.paragraphs
3. No `projectsWithEvolution` Set or registration pattern exists in the codebase
4. The whatNext block on tech/page.tsx uses the server-side `t` variable, not `useTranslations`
</verification>

<success_criteria>
- EVO-01: SageConnect main page renders whatNext section (verified existing, no code change)
- EVO-02: SageSync main page renders whatNext section (JSON content added to both locales)
- EVO-03: Tech pages render whatNext section for projects with content, silently skip for others
- EVO-04: No slug registration or component registry — try/catch pattern is the only detection mechanism
- `npm run build` succeeds with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-evolution-and-discovery/03-01-SUMMARY.md`
</output>
