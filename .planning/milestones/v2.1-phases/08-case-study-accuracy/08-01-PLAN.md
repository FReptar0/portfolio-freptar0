---
phase: 08-case-study-accuracy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - locales/en/projects.json
  - locales/es/projects.json
  - locales/en/caseStudy.json
  - locales/es/caseStudy.json
autonomous: true
requirements: [TRST-02, TRST-03]

must_haves:
  truths:
    - "CarryTrade project card and case study clearly state paper trading status, not production"
    - "CarryTrade tags say 'Paper Trading Experiment' not 'Live Experiment'"
    - "SageConnect case study pages have zero Spring Boot references"
  artifacts:
    - path: "locales/en/projects.json"
      provides: "CarryTrade entry with paper trading language"
      contains: "paper trading"
    - path: "locales/es/projects.json"
      provides: "CarryTrade entry with paper trading language (ES)"
      contains: "paper trading"
    - path: "locales/en/caseStudy.json"
      provides: "CarryTrade case study with explicit paper trading framing"
      contains: "paper trading"
    - path: "locales/es/caseStudy.json"
      provides: "CarryTrade case study with explicit paper trading framing (ES)"
      contains: "paper trading"
  key_links:
    - from: "locales/en/projects.json"
      to: "locales/en/caseStudy.json"
      via: "carrytrade entries match in tone and claims"
      pattern: "paper trading"
---

<objective>
Clarify CarryTrade as a paper trading experiment and verify SageConnect case study has no Spring Boot references.

Purpose: Recruiters must not interpret CarryTrade as a production trading system. SageConnect case study pages must reference the correct tech stack (Node.js/Express, not Spring Boot).
Output: Updated locale JSON files for both EN and ES with honest CarryTrade framing and verified SageConnect content.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-homepage-trust-signals/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify CarryTrade as paper trading experiment in all locale files</name>
  <files>locales/en/projects.json, locales/es/projects.json, locales/en/caseStudy.json, locales/es/caseStudy.json</files>
  <action>
Update CarryTrade content across all 4 locale files to make paper trading status unmistakable:

**locales/en/projects.json** — `items.carrytrade`:
- Change `title` from "Carry Trade Algo - Forex Strategy" to "Carry Trade Algo - Paper Trading Experiment"
- Change `businessProblem` to begin with "An algorithmic trading experiment currently running on OANDA paper trading (no real money at risk)." then continue with the carry trade rationale
- Change tag `"Live Experiment"` to `"Paper Trading"` in the tags array

**locales/es/projects.json** — `items.carrytrade`:
- Change `title` from "Carry Trade Algo - Estrategia Forex" to "Carry Trade Algo - Experimento de Paper Trading"
- Change `businessProblem` to begin with "Un experimento de trading algoritmico ejecutandose actualmente en paper trading de OANDA (sin dinero real en riesgo)." then continue with the carry trade rationale
- Change tag `"Experimento en Vivo"` to `"Paper Trading"` in the tags array

**locales/en/caseStudy.json** — `projects.carrytrade`:
- Change `tagline` from "Algorithmic Forex Strategy — Live 94-Day Validation on $100K" to "Algorithmic Forex Strategy — 94-Day Paper Trading Validation"
- Change tag `"Live Experiment"` to `"Paper Trading"` in the tags array
- The overview already mentions "OANDA paper trading — no real money at risk" — leave it unchanged
- The impact section already says "on OANDA paper trading" — leave it unchanged

**locales/es/caseStudy.json** — `projects.carrytrade`:
- Change `tagline` from "Estrategia Forex Algorítmica — Validación en Vivo de 94 Días con $100K" to "Estrategia Forex Algorítmica — Validación de 94 Días en Paper Trading"
- Change tag `"Experimento en Vivo"` to `"Paper Trading"` in the tags array
- The overview already mentions "paper trading de OANDA — sin dinero real en riesgo" — leave it unchanged
- The impact section already says "en paper trading de OANDA" — leave it unchanged

Do NOT change the case study `overview`, `problem`, `solution`, `impact`, or `tech` sections — they already correctly reference paper trading. Only fix the title, tagline, and tags where "Live" language implies production trading.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && node -e "
const enP = require('./locales/en/projects.json');
const esP = require('./locales/es/projects.json');
const enC = require('./locales/en/caseStudy.json');
const esC = require('./locales/es/caseStudy.json');
const checks = [
  [!enP.items.carrytrade.title.includes('Forex Strategy'), 'EN projects title no longer says Forex Strategy'],
  [enP.items.carrytrade.title.includes('Paper Trading'), 'EN projects title includes Paper Trading'],
  [enP.items.carrytrade.tags.includes('Paper Trading'), 'EN projects tag is Paper Trading'],
  [!enP.items.carrytrade.tags.includes('Live Experiment'), 'EN projects no Live Experiment tag'],
  [esP.items.carrytrade.tags.includes('Paper Trading'), 'ES projects tag is Paper Trading'],
  [!esP.items.carrytrade.tags.includes('Experimento en Vivo'), 'ES projects no Experimento en Vivo tag'],
  [!enC.projects.carrytrade.tagline.includes('Live'), 'EN case study tagline no Live'],
  [enC.projects.carrytrade.tagline.includes('Paper Trading'), 'EN case study tagline includes Paper Trading'],
  [!esC.projects.carrytrade.tagline.includes('Vivo'), 'ES case study tagline no Vivo'],
  [esC.projects.carrytrade.tags.includes('Paper Trading'), 'ES case study tag is Paper Trading'],
];
let pass = true;
checks.forEach(([ok, msg]) => { console.log(ok ? 'PASS' : 'FAIL', msg); if (!ok) pass = false; });
process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <done>CarryTrade title, tagline, businessProblem intro, and tags all clearly state paper trading status in both EN and ES locale files. No language implies live trading of real funds.</done>
</task>

<task type="auto">
  <name>Task 2: Verify SageConnect case study has no Spring Boot references</name>
  <files>locales/en/caseStudy.json, locales/es/caseStudy.json</files>
  <action>
Audit the SageConnect sections in both EN and ES caseStudy.json for any Spring Boot references. Based on the current file analysis, the SageConnect case study already uses Node.js/Express/SQL Server/PM2 — no Spring Boot is present.

Run a grep-based verification to confirm. If any Spring Boot references are found in SageConnect case study content (within the `projects.sageconnect` object), replace them with the correct technology (Node.js, Express, or JavaScript as appropriate).

Phase 7 already fixed Spring Boot references in skills.json and timeline.json. The Phase 7 decision states: "Spring Boot stays in skills list with generic project description — MySQL and Jenkins SageConnect refs are accurate and unchanged." This task only checks the case study files (`caseStudy.json`), not skills or timeline.

Expected outcome: No changes needed — this is a verification task confirming Phase 7 completeness. If unexpected references exist, fix them to reference Node.js/Express.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && node -e "
const fs = require('fs');
const enC = JSON.parse(fs.readFileSync('./locales/en/caseStudy.json', 'utf8'));
const esC = JSON.parse(fs.readFileSync('./locales/es/caseStudy.json', 'utf8'));
const enSC = JSON.stringify(enC.projects.sageconnect);
const esSC = JSON.stringify(esC.projects.sageconnect);
const enHas = enSC.toLowerCase().includes('spring boot');
const esHas = esSC.toLowerCase().includes('spring boot');
console.log(enHas ? 'FAIL EN SageConnect has Spring Boot' : 'PASS EN SageConnect has no Spring Boot');
console.log(esHas ? 'FAIL ES SageConnect has Spring Boot' : 'PASS ES SageConnect has no Spring Boot');
process.exit((enHas || esHas) ? 1 : 0);
"</automated>
  </verify>
  <done>SageConnect case study content in both EN and ES caseStudy.json contains zero Spring Boot references. All tech references are Node.js/Express/SQL Server/PM2.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — no broken translation keys
2. CarryTrade project card and case study explicitly say "Paper Trading" in title/tagline/tags
3. SageConnect case study has zero Spring Boot references in either locale
4. No other content was changed unintentionally (only CarryTrade and SageConnect entries touched)
</verification>

<success_criteria>
- CarryTrade title includes "Paper Trading" in both EN and ES
- CarryTrade tagline no longer says "Live" in either locale
- CarryTrade tags array uses "Paper Trading" instead of "Live Experiment"/"Experimento en Vivo"
- SageConnect case study contains no Spring Boot references in either locale
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-case-study-accuracy/08-01-SUMMARY.md`
</output>
