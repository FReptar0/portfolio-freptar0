---
phase: 08-case-study-accuracy
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - locales/en/projects.json
  - locales/es/projects.json
  - locales/en/caseStudy.json
  - locales/es/caseStudy.json
  - locales/en/skills.json
  - locales/es/skills.json
  - components/sections/Projects.tsx
  - components/case-study/CaseStudyHero.tsx
autonomous: true
requirements: [TRST-01, TRST-04]

must_haves:
  truths:
    - "Every case study hero shows team member roles instead of anonymous headcount"
    - "Every project card on homepage shows team member roles instead of 'Team of N'"
    - "Tech stacks agree across projects.json, caseStudy.json, and skills.json for every project in both EN and ES"
    - "SageSync database reference is consistent (SQL Server) across all locale files"
  artifacts:
    - path: "locales/en/projects.json"
      provides: "Team roles for all projects + consistent tech stacks"
      contains: "teamRoles"
    - path: "locales/es/projects.json"
      provides: "Team roles for all projects (ES) + consistent tech stacks"
      contains: "teamRoles"
    - path: "locales/en/caseStudy.json"
      provides: "Team roles for all case studies"
      contains: "teamRoles"
    - path: "locales/es/caseStudy.json"
      provides: "Team roles for all case studies (ES)"
      contains: "teamRoles"
    - path: "components/sections/Projects.tsx"
      provides: "Renders team roles instead of teamSize"
      contains: "teamRoles"
    - path: "components/case-study/CaseStudyHero.tsx"
      provides: "Renders team roles instead of Team of N"
      contains: "teamRoles"
  key_links:
    - from: "components/sections/Projects.tsx"
      to: "locales/en/projects.json"
      via: "t.raw() reads teamRoles array"
      pattern: "teamRoles"
    - from: "components/case-study/CaseStudyHero.tsx"
      to: "locales/en/caseStudy.json"
      via: "t.raw() reads teamRoles array"
      pattern: "teamRoles"
---

<objective>
Replace anonymous team headcounts with role labels across all case studies and project cards, and fix all cross-content tech stack inconsistencies.

Purpose: Recruiters should see who was on the team (e.g., "Lead Dev, Backend Dev, QA"), not just a number. Tech stacks must agree everywhere a project is mentioned.
Output: Updated locale files with team roles, updated TSX components to render roles, and consistent tech stacks across all locations.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-case-study-accuracy/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 output and existing codebase. -->

From components/sections/Projects.tsx (current):
```typescript
interface ProjectData {
  // ...
  myContribution: {
    role: string;
    teamSize: number;   // WILL BE REPLACED by teamRoles
    responsibilities: string[];
  };
}
// Rendered as: {t('labels.teamOf')} {project.myContribution.teamSize}
```

From components/case-study/CaseStudyHero.tsx (current):
```typescript
const teamSize = t.raw(`projects.${slug}.teamSize`) as number;
// Rendered as: Team of {teamSize}
```

From locales/en/projects.json — current team display pattern per project:
```json
"myContribution": {
  "role": "Lead Developer",
  "teamSize": 4,
  "responsibility0": "..."
}
```

From locales/en/caseStudy.json — current team display pattern per project:
```json
"role": "Lead Developer",
"teamSize": 4,
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add teamRoles arrays to all locale JSON files and fix tech stack inconsistencies</name>
  <files>locales/en/projects.json, locales/es/projects.json, locales/en/caseStudy.json, locales/es/caseStudy.json, locales/en/skills.json, locales/es/skills.json</files>
  <action>
**Part A: Add teamRoles to every project in projects.json (EN and ES)**

Add a `teamRoles` array alongside the existing `teamSize` field in each project's `myContribution` object. Keep `teamSize` for backward compatibility but add `teamRoles` as the display source.

EN `locales/en/projects.json`:
- sageconnect (team of 4): `"teamRoles": ["Lead Developer", "Backend Developer", "QA Engineer", "DevOps"]`
- sagesync (team of 3): `"teamRoles": ["Full-Stack Developer", "Backend Developer", "QA Engineer"]`
- cardeal (team of 4): `"teamRoles": ["Full-Stack Developer", "Backend Developer", "UI Designer", "QA Engineer"]`
- odoo (team of 2): `"teamRoles": ["Python Developer", "Business Analyst"]`
- gymmanager (team of 1): `"teamRoles": ["Solo Developer"]`
- carrytrade (team of 1): `"teamRoles": ["Solo Developer"]`
- cleany (team of 2): `"teamRoles": ["Lead Engineer", "Frontend Developer"]`

ES `locales/es/projects.json`:
- sageconnect: `"teamRoles": ["Desarrollador Líder", "Desarrollador Backend", "Ingeniero QA", "DevOps"]`
- sagesync: `"teamRoles": ["Desarrollador Full-Stack", "Desarrollador Backend", "Ingeniero QA"]`
- cardeal: `"teamRoles": ["Desarrollador Full-Stack", "Desarrollador Backend", "Diseñador UI", "Ingeniero QA"]`
- odoo: `"teamRoles": ["Desarrollador Python", "Analista de Negocios"]`
- gymmanager: `"teamRoles": ["Desarrollador Individual"]`
- carrytrade: `"teamRoles": ["Desarrollador Individual"]`
- cleany: `"teamRoles": ["Ingeniero Líder", "Desarrollador Frontend"]`

**Part B: Add teamRoles to every case study in caseStudy.json (EN and ES)**

Add `teamRoles` array alongside the existing `teamSize` field in each case study project.

EN `locales/en/caseStudy.json` — same roles as projects.json:
- sageconnect: `"teamRoles": ["Lead Developer", "Backend Developer", "QA Engineer", "DevOps"]`
- sagesync: `"teamRoles": ["Full-Stack Developer", "Backend Developer", "QA Engineer"]`
- cardeal: `"teamRoles": ["Full-Stack Developer", "Backend Developer", "UI Designer", "QA Engineer"]`
- odoo: `"teamRoles": ["Python Developer", "Business Analyst"]`
- gymmanager: `"teamRoles": ["Solo Developer"]`
- carrytrade: `"teamRoles": ["Solo Developer"]`
- cleany: `"teamRoles": ["Lead Engineer", "Frontend Developer"]`

ES `locales/es/caseStudy.json` — same translated roles as ES projects.json above.

**Part C: Fix cross-content tech stack inconsistencies**

These are the known inconsistencies discovered during planning. Fix them so every mention of a project's tech stack agrees:

1. **SageSync database mismatch (ES projects.json):** ES `locales/es/projects.json` shows `"PostgreSQL"` in sagesync stack but EN shows `"SQL Server"`. The case study (both EN and ES) also says `"SQL Server"`. Fix ES projects.json sagesync stack to use `"SQL Server"` instead of `"PostgreSQL"` to match the case study and EN version.

2. **SageConnect projects.json stack is too sparse:** EN and ES `projects.json` shows `["JavaScript", "SQL Server", "Docker"]` but the case study tech deep-dive shows `["Node.js", "Express", "SQL Server", "PM2"]`. Update `projects.json` sageconnect technicalSolution.stack to `["Node.js", "Express", "SQL Server", "Docker"]` in both EN and ES. Note: Keep Docker (from projects.json, it IS used) but replace "JavaScript" with the more specific "Node.js" and add "Express". Do NOT add PM2 to projects.json — it is an operational detail best left to the tech deep-dive.

3. **Skills.json MySQL-SageConnect association:** `skills.json` (EN) says MySQL is for "SageConnect transaction processing and ERP data" but SageConnect actually uses SQL Server. Change the MySQL project field to "Enterprise data management and relational database systems" in EN, and "Gestión de datos empresariales y sistemas de bases de datos relacionales" in ES. This removes the false SageConnect-MySQL association while keeping MySQL as a legitimate skill.

4. **Skills.json PostgreSQL-SageSync association:** `skills.json` (EN) says PostgreSQL is for "SageSync inventory management and analytics" but SageSync uses SQL Server. Change the PostgreSQL project field to "Cleany multi-tenant SaaS and Gym Manager operations platform" in EN, and "Cleany SaaS multi-tenant y plataforma de operaciones Gym Manager" in ES. These are actual projects that use PostgreSQL.

Do NOT touch `trustSignals.json` — Phase 7 decision keeps Spring Boot in the general technologies list.
Do NOT touch `timeline.json` — Phase 7 already fixed it.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && node -e "
const enP = require('./locales/en/projects.json');
const esP = require('./locales/es/projects.json');
const enC = require('./locales/en/caseStudy.json');
const esC = require('./locales/es/caseStudy.json');
const enS = require('./locales/en/skills.json');
const esS = require('./locales/es/skills.json');
let pass = true;
const check = (ok, msg) => { console.log(ok ? 'PASS' : 'FAIL', msg); if (!ok) pass = false; };

// TRST-01: teamRoles exist
const slugs = ['sageconnect','sagesync','cardeal','odoo','gymmanager','carrytrade','cleany'];
slugs.forEach(s => {
  check(Array.isArray(enP.items[s]?.myContribution?.teamRoles), 'EN projects ' + s + ' has teamRoles');
  check(Array.isArray(esP.items[s]?.myContribution?.teamRoles), 'ES projects ' + s + ' has teamRoles');
});
const csSlugs = ['sageconnect','sagesync','cardeal','odoo','gymmanager','carrytrade','cleany'];
csSlugs.forEach(s => {
  check(Array.isArray(enC.projects[s]?.teamRoles), 'EN caseStudy ' + s + ' has teamRoles');
  check(Array.isArray(esC.projects[s]?.teamRoles), 'ES caseStudy ' + s + ' has teamRoles');
});

// TRST-04: SageSync uses SQL Server everywhere
check(enP.items.sagesync.technicalSolution.stack.includes('SQL Server'), 'EN projects SageSync has SQL Server');
check(esP.items.sagesync.technicalSolution.stack.includes('SQL Server'), 'ES projects SageSync has SQL Server');
check(!esP.items.sagesync.technicalSolution.stack.includes('PostgreSQL'), 'ES projects SageSync no PostgreSQL');

// TRST-04: SageConnect projects.json has Node.js
check(enP.items.sageconnect.technicalSolution.stack.includes('Node.js'), 'EN projects SageConnect has Node.js');
check(esP.items.sageconnect.technicalSolution.stack.includes('Node.js'), 'ES projects SageConnect has Node.js');
check(enP.items.sageconnect.technicalSolution.stack.includes('Express'), 'EN projects SageConnect has Express');

// TRST-04: skills.json MySQL not SageConnect
check(!enS.categories[2].skills[0].project.includes('SageConnect'), 'EN skills MySQL not SageConnect');
check(!esS.categories[2].skills[0].project.includes('SageConnect'), 'ES skills MySQL not SageConnect');

// TRST-04: skills.json PostgreSQL not SageSync
check(!enS.categories[2].skills[1].project.includes('SageSync'), 'EN skills PostgreSQL not SageSync');
check(!esS.categories[2].skills[1].project.includes('SageSync'), 'ES skills PostgreSQL not SageSync');

process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <done>All 7 projects have teamRoles arrays in both projects.json and caseStudy.json for EN and ES. Tech stack inconsistencies fixed: SageSync uses SQL Server everywhere, SageConnect projects card matches case study, skills.json database associations are accurate.</done>
</task>

<task type="auto">
  <name>Task 2: Update Projects.tsx and CaseStudyHero.tsx to render team roles instead of headcounts</name>
  <files>components/sections/Projects.tsx, components/case-study/CaseStudyHero.tsx</files>
  <action>
**Projects.tsx changes:**

1. Update the `ProjectData` interface: add `teamRoles: string[]` to the `myContribution` type (keep `teamSize` for now).

2. In each project data object (sageconnect, sagesync, cardeal, etc.), add:
   ```typescript
   teamRoles: (t.raw('items.{slug}.myContribution.teamRoles') as string[]),
   ```

3. Replace the team size display (currently around line 371):
   ```
   {t('labels.teamOf')} {project.myContribution.teamSize}
   ```
   With a comma-separated list of roles:
   ```tsx
   <span className="font-mono text-sm" style={{ color: 'var(--text-secondary)' }}>
     {project.myContribution.teamRoles.join(' / ')}
   </span>
   ```
   Use " / " as separator for readability. The `teamOf` label is no longer needed here.

**CaseStudyHero.tsx changes:**

1. Replace the teamSize read (line 22):
   ```typescript
   const teamSize = t.raw(`projects.${slug}.teamSize`) as number;
   ```
   With:
   ```typescript
   const teamRoles = t.raw(`projects.${slug}.teamRoles`) as string[];
   ```

2. Replace the team display (line 60-62):
   ```tsx
   <span className="inline-flex items-center gap-1.5 text-sm text-foreground/60">
     <Users className="w-4 h-4" />
     Team of {teamSize}
   </span>
   ```
   With:
   ```tsx
   <span className="inline-flex items-center gap-1.5 text-sm text-foreground/60">
     <Users className="w-4 h-4" />
     {teamRoles.join(' / ')}
   </span>
   ```

Keep the `Users` icon — it still makes sense for showing team composition. The display changes from "Team of 4" to "Lead Developer / Backend Developer / QA Engineer / DevOps".
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Projects.tsx renders team roles (e.g., "Lead Developer / Backend Developer / QA Engineer / DevOps") instead of "Team of 4" for every project card. CaseStudyHero.tsx renders team roles instead of "Team of N" for every case study hero. Build passes with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes — all pages generate without broken translation keys
2. Every project card shows role labels instead of "Team of N"
3. Every case study hero shows role labels instead of "Team of N"
4. Tech stacks are consistent across projects.json and caseStudy.json for every project
5. SageSync uses SQL Server consistently across all files in both EN and ES
6. SageConnect projects card tech stack matches the case study tech deep-dive
7. Skills.json database entries reference correct projects
</verification>

<success_criteria>
- All 7 projects have teamRoles arrays in both EN and ES locale files (projects.json + caseStudy.json)
- Components render roles, not headcounts
- Zero cross-content tech stack mismatches between projects.json, caseStudy.json, and skills.json
- SageSync database is "SQL Server" in all locations, both languages
- SageConnect projects card includes "Node.js" and "Express"
- `npm run build` succeeds with all pages generated
</success_criteria>

<output>
After completion, create `.planning/phases/08-case-study-accuracy/08-02-SUMMARY.md`
</output>
