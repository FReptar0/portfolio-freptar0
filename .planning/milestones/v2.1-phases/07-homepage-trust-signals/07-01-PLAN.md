---
phase: 07-homepage-trust-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/Hero.tsx
  - locales/en/hero.json
  - locales/es/hero.json
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04

must_haves:
  truths:
    - "Hero displays '6+ Systems Shipped' instead of '20+ Projects Shipped'"
    - "Hero displays a measurable error/time reduction percentage instead of '$2M+ Value Delivered'"
    - "Hero displays '300+ Users Impacted' as a metric"
    - "Hero displays an accurate years-of-experience figure based on 2021 start date"
    - "Both EN and ES locale files have matching, updated metric labels"
  artifacts:
    - path: "components/sections/Hero.tsx"
      provides: "Updated hardcoded metric values"
      contains: "300+"
    - path: "locales/en/hero.json"
      provides: "English metric labels for new metrics"
      contains: "Systems Shipped"
    - path: "locales/es/hero.json"
      provides: "Spanish metric labels for new metrics"
      contains: "Sistemas Enviados"
  key_links:
    - from: "components/sections/Hero.tsx"
      to: "locales/{en,es}/hero.json"
      via: "useTranslations('hero') for metric labels"
      pattern: "t\\('metrics\\."
---

<objective>
Replace all inflated and fabricated Hero metrics with honest, verifiable numbers.

Purpose: The Hero section is the first thing a recruiter sees. Currently it shows "5+ Years Experience", "20+ Projects Shipped", and "$2M+ Value Delivered" -- the last two are inflated/unverifiable. Replace with truthful metrics: "6+ Systems Shipped", "300+ Users Impacted", a measurable error/time reduction %, and verify years accuracy.

Output: Updated Hero.tsx with correct hardcoded values, updated EN and ES hero.json with new metric labels.
</objective>

<execution_context>
@/Users/freptar0/.claude/get-shit-done/workflows/execute-plan.md
@/Users/freptar0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@components/sections/Hero.tsx
@locales/en/hero.json
@locales/es/hero.json

<interfaces>
<!-- Hero.tsx currently renders metrics with hardcoded values and translated labels -->
<!-- Lines 47-49 in Hero.tsx: -->
```tsx
<MetricItem value="5+" label={t('metrics.experience')} />
<MetricItem value="20+" label={t('metrics.projects')} />
<MetricItem value="$2M+" label={t('metrics.value')} />
```

<!-- hero.json metric keys (EN): -->
```json
"metrics": {
  "experience": "Years Experience",
  "projects": "Projects Shipped",
  "value": "Value Delivered"
}
```

<!-- hero.json metric keys (ES): -->
```json
"metrics": {
  "experience": "Anos de Experiencia",
  "projects": "Proyectos Enviados",
  "value": "Valor Entregado"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hero metric values and locale labels</name>
  <files>components/sections/Hero.tsx, locales/en/hero.json, locales/es/hero.json</files>
  <action>
  In `components/sections/Hero.tsx`, replace the three MetricItem lines (47-49) with FOUR metrics:

  ```tsx
  <MetricItem value="5+" label={t('metrics.experience')} />
  <MetricItem value="6+" label={t('metrics.systems')} />
  <MetricItem value="300+" label={t('metrics.users')} />
  <MetricItem value="99%" label={t('metrics.accuracy')} />
  ```

  Rationale for values:
  - "5+" years: Timeline starts 2021, current year 2026 = 5 years. "5+" is accurate. Keep as-is.
  - "6+" systems shipped: Per user direction (SageConnect, SageSync, Qardeal, GymManager, Inova/Odoo, portfolio = 6+)
  - "300+" users impacted: Per user direction
  - "99%" accuracy: Represents the error reduction metric (manual errors reduced to near-zero). Use "99%" as the value since SageConnect description says "reducing manual errors to zero"

  In `locales/en/hero.json`, replace the `metrics` object:
  ```json
  "metrics": {
    "experience": "Years Experience",
    "systems": "Systems Shipped",
    "users": "Users Impacted",
    "accuracy": "Error Reduction"
  }
  ```

  In `locales/es/hero.json`, replace the `metrics` object:
  ```json
  "metrics": {
    "experience": "Anhos de Experiencia",
    "systems": "Sistemas Enviados",
    "users": "Usuarios Impactados",
    "accuracy": "Reduccion de Errores"
  }
  ```

  Note: Keep the proper Spanish characters (tildes, accents) -- use the exact same accent patterns already in the file. The ES file currently uses "Anos de Experiencia" (check actual file for accent usage and match it).

  Remove the old keys `projects` and `value` from both locale files since they are no longer referenced.
  </action>
  <verify>
    <automated>cd /Users/freptar0/Desktop/Projects/portfolio-freptar0 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - Hero.tsx renders exactly 4 MetricItem components with values "5+", "6+", "300+", "99%"
  - No references to old metric keys (metrics.projects, metrics.value) remain in Hero.tsx
  - EN hero.json has keys: experience, systems, users, accuracy (no projects or value)
  - ES hero.json has matching keys with Spanish translations
  - Build passes with no errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `grep -n "20+" components/sections/Hero.tsx` returns nothing (old value removed)
- `grep -n "\\$2M" components/sections/Hero.tsx` returns nothing (old value removed)
- `grep "projects" locales/en/hero.json` returns nothing (old key removed)
- `grep "value" locales/en/hero.json` returns nothing (old key removed)
- Both EN and ES hero.json files have identical metric key names (experience, systems, users, accuracy)
</verification>

<success_criteria>
The Hero section displays four honest metrics: "5+ Years Experience", "6+ Systems Shipped", "300+ Users Impacted", and "99% Error Reduction" -- all verifiable claims with matching EN/ES translations.
</success_criteria>

<output>
After completion, create `.planning/phases/07-homepage-trust-signals/07-01-SUMMARY.md`
</output>
